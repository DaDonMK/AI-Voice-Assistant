import {makeStyles} from '@material-ui/core/styles'

export default makeStyles({
    container:  {
        padding: '50 5%',
        width: '100%',
        margin: 50
    }
})

// export default styles